Hi <%= params[:user].email %>,

Someone initiated password reset. If that was you then use below link to reset it.

<%= link_to "Reset Password", password_reset_edit_url(token: @token) %>

P.S - This link will expire in 15 minutes.
